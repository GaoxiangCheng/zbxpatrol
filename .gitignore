# 构建产物 / build artifacts
/target
/dist

# 运行时配置与输出（含敏感信息，不入库）/ runtime config & outputs (sensitive)
.env
patrol.toml
reports/
*.xlsx
*.csv
*.log

# 系统与编辑器 / OS & editors
.DS_Store
*.swp
.idea
.vscode
zbx
